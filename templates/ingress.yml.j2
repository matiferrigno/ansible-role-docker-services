# Generated by Ansible
#   role: matiferrigno.docker-services

networks:
  {{ docker_services_proxy_network }}:
    external: true
    name: {{ docker_services_proxy_network }}

services:
{% for i in item.ingress | default([]) %}
  {{ i.service }}:
    networks:
      - {{ docker_services_proxy_network }}
    labels:
      - "traefik.enable={{ ((i.state | default('present')) == 'absent') | ternary(false,true) }}"
{% if i.state | default('present') != 'absent' -%}
{% for h in i.hosts | default([]) %}
{% if h.state | default('present') != 'absent' %}
      - "traefik.http.routers.{{ h.host | replace('.','-') }}-rtr.entrypoints={{ h.entrypoints | default(['https'] | join(',')) }}"
      - "traefik.http.routers.{{ h.host | replace('.','-') }}-rtr.rule=Host(`{{ h.host }}`)"
{% for m in h.middlewares | default([]) %}
       - "traefik.http.routers.{{ h.host | replace('.','-') }}-rtr.middlewares={{ m }}"
{% endfor %}
      - "traefik.http.routers.{{ h.host | replace('.','-') }}-rtr.service={{ h.host | replace('.','-') }}-svc"
      - "traefik.http.services.{{ h.host | replace('.','-') }}-svc.loadbalancer.server.port={{ h.port | default(80) }}"
{% endif %}
{% endfor -%}
{% endif %}
{% endfor %}
